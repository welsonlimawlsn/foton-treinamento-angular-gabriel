import { StatusClientePipe } from './status-cliente.pipe';

describe('StatusClientePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusClientePipe();
    expect(pipe).toBeTruthy();
  });
});
