import { EstadoContaPipe } from './estado-conta.pipe';

describe('EstadoContaPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoContaPipe();
    expect(pipe).toBeTruthy();
  });
});
