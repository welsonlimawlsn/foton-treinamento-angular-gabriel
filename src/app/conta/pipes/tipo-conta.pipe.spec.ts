import { TipoContaPipe } from './tipo-conta.pipe';

describe('TipoContaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoContaPipe();
    expect(pipe).toBeTruthy();
  });
});
